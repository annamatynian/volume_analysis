# volume_analysis — зависимости проекта
# Создано: 2026-04-09
#
# Установка в виртуальное окружение:
#   python -m venv D:\DeFi-RAG-Projects\volume_analysis\venv
#   D:\DeFi-RAG-Projects\volume_analysis\venv\Scripts\activate
#   pip install -r requirements.txt

# --- Ядро анализа ---
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3       # HDBSCAN появился в sklearn 1.3 (нужен для ЗАДАЧА 2)
scipy>=1.10             # find_peaks используется в extract_sub_levels()

# --- Кластеризация (резерв если sklearn < 1.3) ---
# hdbscan                # раскомментировать если sklearn < 1.3

# --- Биржа и данные ---
ccxt>=4.0               # Binance futures API
aiohttp>=3.8            # async HTTP для onchain_client.py

# --- Parquet кэш ---
pyarrow>=12.0           # parquet read/write в delta_cache.py

# --- Тесты ---
pytest>=7.0
pytest-asyncio>=0.21    # async тесты если нужны

# --- Утилиты ---
pytz>=2023.3            # timezone handling (conftest.py фильтрует DeprecationWarning)
requests>=2.28          # HTTP download в download_anchor_data.py
