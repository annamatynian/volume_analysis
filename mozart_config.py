# mozart_config.py
# Числовые пороги паттернов трейдера Mozart.
# Источник каждого порога указан — менять только при обновлении PLAN_MOZART_PATTERNS.md.
# НЕ хардкодить значения из этого файла в production-коде или в assertions тестов.
# При изменении рыночной фазы меняется только этот файл — логика и тесты не трогаются.

MOZART_CONFIG: dict = {

    # ── Н-01 | RSI(14) дневного ТФ ──────────────────────────────────────────
    # Источник: пост 05.02.2026 (Н-01, PLAN_MOZART_PATTERNS.md ЧАСТЬ 3)
    # RSI < extreme_oversold → EXTREME_OVERSOLD (прецедент: коронадамп 2020)
    # RSI < oversold         → OVERSOLD
    # иначе                  → NEUTRAL
    "rsi_extreme_oversold": 15,
    "rsi_oversold": 20,

    # ── Н-02 | Красные месячные свечи подряд ────────────────────────────────
    # Источник: пост 11.02.2026 (Н-02, PLAN_MOZART_PATTERNS.md ЧАСТЬ 3)
    # N >= red_months_rare    → редкость, повышенная вероятность отскока
    # N >= red_months_extreme → единственный прецедент BTC: авг–дек 2018
    # ВАЖНО: N >= extreme ≠ конец медвежки; указывает на экстремальную перепроданность
    "red_months_rare": 4,
    "red_months_extreme": 5,

    # ── МБ-03 | % STH в прибыли (profit-loss endpoint) ──────────────────────
    # Источник: пост 09.09.2025 + пост 16.04.2026 (МБ-03, ЧАСТЬ 2)
    # Линии плавающие (синусоидальные кривые), диапазоны из поста 09.09.2025.
    # sth_profit_bear_max     → граница медвежьей зоны (< 51% = большинство STH в убытке)
    # sth_profit_neutral_*    → синяя линия (нейтральная зона, 51–59%)
    # sth_profit_heated_*     → жёлтая линия (перегрев, 69–76%)
    # sth_profit_euphoria_*   → красная линия (Over-heated / эйфория, 85–95%)
    "sth_profit_bear_max": 51,
    "sth_profit_neutral_min": 51,
    "sth_profit_neutral_max": 59,
    "sth_profit_heated_min": 69,
    "sth_profit_heated_max": 76,
    "sth_profit_euphoria_min": 85,
    "sth_profit_euphoria_max": 95,
    # Окно для расчёта дельты (is_dropping_sharply): пост 09.09.2025
    "sth_profit_speed_window_days": 7,
    # FORMALIZED: Mozart описывает «резкое снижение» качественно (пост 09.09.2025).
    # 5 п.п. за 7 дней — наша формализация: колебание внутри синей линии (~1-2 п.п.) не считается.
    # Пробой синей линии вниз за неделю (51%→45%) = резкий = is_dropping_sharply True.
    "sth_profit_drop_threshold_7d": 5.0,

    # ── МБ-04 | Supply in Loss (supply-loss endpoint) ────────────────────────
    # Источник: пост 02.04.2026 + пост 08.04.2026 (МБ-04, ЧАСТЬ 2)
    # structural_trigger  → 5M BTC: исторически = структурная смена тренда
    # intermediate_trigger → 3.5M BTC: «активное» давление монет над ценой
    "supply_loss_structural_trigger": 5_000_000,
    "supply_loss_intermediate_trigger": 3_500_000,

    # ── МБ-05 | LTH Realized Profit 7d MA ───────────────────────────────────
    # Источник: пост 14.01.2026 (МБ-05, ЧАСТЬ 2)
    # MA > lth_profit_7d_ma_warning → HIGH давление распределения (медвежий риск)
    "lth_profit_7d_ma_warning": 1_000_000_000,   # USD/день
    "lth_profit_ma_window_days": 7,

    # ── МБ-08 | One-Cycle Holders Average (realized-cap-hodl-waves) ──────────
    # Источник: пост 13.05.2026 (МБ-08, ЧАСТЬ 2)
    # Цель дна = One_Cycle_RP × (1 - depth_min) … × (1 - depth_max)
    # Продолжительность медвежки: 9–17 месяцев, медиана ~13
    "one_cycle_bear_depth_min": 0.50,
    "one_cycle_bear_depth_max": 0.55,
    "one_cycle_bear_duration_min_months": 9,
    "one_cycle_bear_duration_max_months": 17,
    # one_cycle_bear_confirmed_days: Mozart (пост 13.05.2026):
    #   «Техническая медвежка становится настоящей, если длительность нахождения цены
    #   ниже средней покупки держателей цикла составила более 2-ух месяцев»
    #   60 дней ≈ 2 месяца; при уточнении — менять только здесь.
    "one_cycle_bear_confirmed_days": 60,

    # ── М-01 | LTH SOPR (пост 05.04.2026) ────────────────────────────────────────────
    # Источник: PLAN_MOZART_PATTERNS.md М-01 (посты 05.04.2026)
    # rubicon    = 1.0:  граница бычий↔медвежий; sopr >= 1.0 = рубикон не пробит
    # early_bear = 0.80: нижняя граница «ранней медвежки» (0–20% убыток LTH)
    # deep_bear  = 0.50: начало кульминации (40–50% убыток, скорр. цикл)
    "lth_sopr_rubicon":    1.0,
    "lth_sopr_early_bear": 0.80,
    "lth_sopr_deep_bear":  0.50,

    # ── М-10 | LTH Realized Loss (пост 02.04.2026) ──────────────────────────
    # Источник: пост 02.04.2026 (М-10, PLAN_MOZART_PATTERNS.md ЧАСТЬ 1)
    # Якоря из реализованных убытков LTH на исторических рыночных экстремумах.
    # anchor_2018:         завершение медвежки 2018 ($140M/день)
    # anchor_2022_w1:      начало капитуляции 2022 ($300M/день)
    # anchor_2022_ftx:     пик краша FTX ($480M/день) — исторический максимум
    # anchor_cycle_target: ожидаемый пик текущего цикла ($500M/день)
    "lth_loss_anchor_2018":          140_000_000,
    "lth_loss_anchor_2022_w1":       300_000_000,
    "lth_loss_anchor_2022_ftx":      480_000_000,
    "lth_loss_anchor_cycle_target":  500_000_000,

    # ── М-05 | LTH NUPL (посты 05.04.2026, 15.05.2026) ───────────────────────────────
    # Источник: PLAN_MOZART_PATTERNS.md М-05.
    # euphoria = 0.75: порог эйфории LTH → риск распределения.
    #   ⚠️ Mozart явно не называет 0.75 для LTH NUPL в этих постах.
    #   Значение взято из реализации [LTH PAIN PROXY]; при уточнении — обновить здесь.
    # rubicon = 0.0: граница прибыль↔убыток; Mozart: «Рубиконом является безубыток»
    #   (пост 05.04.2026, аналогия с SOPR). Пересечение нуля = событие, не просто порог.
    # rubicon_eps = 0.005: ширина RUBICON-зоны (±0.5% — шум дневных данных).
    #   Значения в диапазоне [rubicon - eps, rubicon + eps] остаются RUBICON,
    #   не триггерят переход в POSITIVE/BEAR из-за дневного шума.
    "lth_nupl_euphoria":    0.75,
    "lth_nupl_rubicon":     0.0,
    "lth_nupl_rubicon_eps": 0.005,

    # ── М-02 | STH SOPR (пост 16.04.2026) ──────────────────────────────────────────
    # Источник: PLAN_MOZART_PATTERNS.md М-02.
    # rubicon = 1.0: безубыток STH = «нейтральная зона».
    # Mozart: «сильная поддержка на бычьих, сильное сопротивление на медвежьих»
    #   (пост 16.04.2026).
    # rubicon_eps = 0.005: ширина RUBICON-зоны (±0.5% — шум дневных данных).
    # FORMALIZED: Mozart явно не называет eps для STH SOPR.
    #   Значение по аналогии с lth_nupl_rubicon_eps (0.005).
    #   При уточнении — обновить только здесь.
    "sth_sopr_rubicon":     1.0,
    "sth_sopr_rubicon_eps": 0.005,  # FORMALIZED

    # ── М-11 | ETF Flow (пост 08.04.2026) ──────────────────────────────────
    # Источник: PLAN_MOZART_PATTERNS.md М-11 (пост 08.04.2026)
    # Mozart оперирует бинарно: «притоки есть / нет». Нейтральная зона — наш выбор.
    # Контракт границы (FORMALIZED): flow > significant → INFLOW (строгий >);
    #   flow == significant → NEUTRAL (не пересекает порог снизу).
    # 500 BTC/день (~$40M при $80k) отсекает дневной шум.
    # При уточнении Mozart — обновить только здесь; логика и тесты не трогаются.
    "etf_flow_significant_btc": 500,   # BTC/день — FORMALIZED

    # ── М-03 | LTH MVRV (пост 25.02.2026) ──────────────────────────────────
    # Источник: PLAN_MOZART_PATTERNS.md М-03 (пост 25.02.2026)
    # Бинарный рубикон: mvrv >= 1.0 = LTH в нереализованной прибыли (BULL);
    #   mvrv < 1.0 = LTH в убытке, вынужденные продажи снижаются (BEAR).
    # Граница включительно: mvrv == 1.0 → BULL (рубикон не пробит вниз).
    # Нет eps: LTH MVRV — агрегированная метрика, шум менее значим, чем STH.
    "lth_mvrv_rubicon": 1.0,

    # ── М-04 | STH MVRV (пост 16.04.2026) ──────────────────────────────────
    # Источник: PLAN_MOZART_PATTERNS.md М-04 (пост 16.04.2026)
    # Рубикон 1.0 с eps-буфером: STH MVRV волатильнее LTH (дневной шум ~2%).
    # Зоны:
    #   mvrv >  rubicon + eps → BULL    — STH продают с прибылью, давление продаж
    #   mvrv >= rubicon - eps → NEUTRAL — STH у безубытка, нейтральная зона
    #   mvrv <  rubicon - eps → BEAR    — STH капитулируют, убытки зафиксированы
    # Верхняя граница включительно: mvrv == rubicon + eps → NEUTRAL (не BULL).
    # Нижняя граница включительно: mvrv == rubicon - eps → NEUTRAL (не BEAR).
    # FORMALIZED: Mozart не называет eps для STH MVRV явно (пост 16.04.2026).
    #   0.02 = ±2% типичный дневной шум STH MVRV. При уточнении — обновить здесь.
    "sth_mvrv_rubicon":     1.0,
    "sth_mvrv_rubicon_eps": 0.02,   # FORMALIZED: ±2% шум STH MVRV

    # ── М-06 | STH NUPL (пост 16.04.2026) ──────────────────────────────────
    # Источник: PLAN_MOZART_PATTERNS.md М-06 (пост 16.04.2026)
    # Рубикон 0.0: STH когорта пересекает безубыток → смена режима давления.
    # Mozart: «запас на рост по-прежнему имеет место» при NUPL > 0.
    # Асимметричный буфер: eps защищает только НИЖНЮЮ границу RUBICON-зоны.
    #   POSITIVE     : nupl >  rubicon (0.0)         — строгий > (нет eps сверху)
    #   RUBICON      : nupl >= rubicon - eps (-0.005) — включает сам рубикон (0.0)
    #   CAPITULATION : nupl <  rubicon - eps (-0.005) — когорта STH в убытке
    # Верхняя граница RUBICON включительно: nupl == 0.0 → RUBICON (не POSITIVE).
    # Нижняя граница RUBICON включительно: nupl == -eps → RUBICON (не CAPITULATION).
    # FORMALIZED: Mozart не называет eps для STH NUPL явно (пост 16.04.2026).
    #   0.005 = ±0.5% типичный шум дневных on-chain данных. При уточнении — здесь.
    "sth_nupl_rubicon":     0.0,
    "sth_nupl_rubicon_eps": 0.005,  # FORMALIZED: ±0.5% шум дневных on-chain данных

    # ── М-09 | STH Realized Price — Z-score turning (пост 05.04.2026) ────────
    # Источник: PLAN_MOZART_TDD_LEVEL2.md ВЕТКА 10
    # Число дней окна для детекции смены направления Z-score STH RP.
    # Аналогичен окну detect_lth_sopr_turning (паттерн В, ВЕТКА 1).
    # При изменении горизонта наблюдения — менять только здесь.
    "sth_rp_zscore_turning_window": 5,

    # Гейт 1: Z-score STH RP должен быть ниже этого порога (STH RP под давлением).
    # FORMALIZED: порог -1.0 = ~1 стандартное отклонение ниже 90-дн. нормы.
    # При Z >= gate сигнал бессмыслен (STH RP не под давлением).
    "sth_rp_zscore_gate": -1.0,

    # Гейт 2: BTC rolling 7d std дневных доходностей < порога → цена в боковике.
    # FORMALIZED: 2.0% = нижняя граница «нормальной» BTC волатильности;
    # типичная BTC vol: 3–5%/день. < 2% = консолидация (Mozart: «при стоячей цене»).
    # При vol >= порога цена активно движется — паттерн В неприменим.
    "sth_rp_btc_vol_7d_pct_max": 2.0,

    # ── МБ-01 | Realized Price — «Синяя линия» дна цикла ─────────────────────
    # Источник: пост 25.02.2026 (МБ-01, PLAN_MOZART_PATTERNS.md ЧАСТЬ 5)
    # Mozart: «Синяя линия Realized Price ~$54,500» — совпадает с дном циклов
    # 2015, 2018, 2020. «Самый сильный уровень из трёхлинейной модели».
    # ±20% буфер FORMALIZED: защищает от ложных пробоев (FTX-краш 2022 — цена
    # кратко ныряла ниже RP и возвращалась).
    # AT-зона: price в [RP*(1-buf), RP*(1+buf)] — «в зоне синей линии».
    # При уточнении Mozart — менять только здесь; функция и тесты не трогаются.
    "realized_price_buffer_pct": 0.20,   # FORMALIZED: ±20%

    # ── МБ-06 | Общий NUPL — зоны медвежьего рынка (пост 15.05.2026) ─────────
    # Источник: пост 15.05.2026 (МБ-06, PLAN_MOZART_PATTERNS.md ЧАСТЬ 2)
    # API: /v1/nupl → поле nupl (float, десятичная дробь; 0.3004 = 30.04%).
    # Диагностика 20.05.2026: 200 OK, 1460 записей.
    #
    # nupl_bull_threshold:  FORMALIZED — стандартная граница эйфории/распределения.
    #   Mozart явно не называет порог 0.50 для общего NUPL в этих постах.
    #   При уточнении — менять только здесь.
    # nupl_hope_threshold:  FORMALIZED — граница HOPE/EARLY_BEAR.
    #   Mozart's пик ранней медвежки ~18% (0.18) < 0.25, т.е. попадает в EARLY_BEAR.
    #   При уточнении — менять только здесь.
    # nupl_bottom_target:   Mozart, пост 15.05.2026:
    #   «гипотетическое дно в текущем цикле может быть поставлено в районе
    #   40%-ой доли нереализованных убытков в среднем по месяцу»
    #   = NUPL ≈ -0.40 (40% убытков от капитализации).
    "nupl_bull_threshold":  0.50,
    "nupl_hope_threshold":  0.25,
    "nupl_bottom_target":  -0.40,

    # ── МБ-07 | MVRV Z-Score — макро позиционирование (пост 25.02.2026) ────────
    # Источник: пост 25.02.2026 (МБ-07, PLAN_MOZART_PATTERNS.md ЧАСТЬ 2)
    # API: /v1/mvrv-zscore → поле mvrvZscore (float).
    # Диагностика 20.05.2026: 200 OK, 1460 записей, диапазон [-0.36, 3.35].
    #
    # mvrv_zscore_peak:   стандартный порог on-chain экстремума (истор. топы цикла).
    #   Mozart явно не называет 7.0 в этих постах; при уточнении — менять здесь.
    #   В данных BGeometrics (2022–2026) не достигается (макс 3.35).
    # mvrv_zscore_bull:   зона сильного бычьего рынка; FORMALIZED.
    # mvrv_zscore_bottom: Mozart (пост 25.02.2026): «Z < 0 = рынок ниже
    #   realized price = зона исторического дна».
    #   BEAR: немного ниже 0 (0 > z > bottom).
    #   BOTTOM: значительно ниже Realized Price (z <= bottom).
    "mvrv_zscore_peak":    7.0,
    "mvrv_zscore_bull":    3.0,
    "mvrv_zscore_bottom": -1.0,

    # ── НВ-01 | Регрессия убывающих пиков (пост 31.03.2026) ─────────────────────
    # Источник: пост 31.03.2026 (НВ-01, PLAN_MOZART_PATTERNS.md ЧАСТЬ 4)
    # Паттерн Mozart: серия убывающих локальных максимумов 87k → 84k → 76k → (63–67k).
    # Алгоритм: swing high по high свечи, монотонная проверка + linregress.
    #
    # swing_high_window: окно в днях для детекции локального максимума.
    #   Пик i подтверждён если high[i] строго выше левого окна и не ниже правого.
    #   n=25 ≈ 1 торговый месяц; захватывает циклические пики без внутринедельного шума.
    #   FORMALIZED: Mozart не называет конкретное окно. При уточнении — менять здесь.
    # swing_high_lookback_peaks: сколько последних пиков брать для регрессии.
    #   K=5 — достаточно для линейной регрессии, не захватывает пики из предыдущего цикла.
    #   FORMALIZED: при изменении горизонта анализа — менять только здесь.
    # max_days_between_peaks: максимальный допустимый интервал между соседними пиками.
    #   Пики с бо́льшим интервалом принадлежат разным рыночным «эпохам» и не должны
    #   объединяться в одну регрессию. Gemini validation 2026-06-01: рекомендовано 60–90.
    #   FORMALIZED: 90 дней ≈ 3 месяца. При уточнении — менять только здесь.
    "swing_high_window":         25,
    "swing_high_lookback_peaks": 5,
    "max_days_between_peaks":    90,

    # ── НВ-03 | BTC Dominance — ротация ликвидности (пост 10.05.2026) ──────────
    # Источник: пост 10.05.2026 (НВ-03, PLAN_MOZART_PATTERNS.md ЧАСТЬ 4)
    # API: CoinGecko /api/v3/global → поле btc_dominance (float, %)
    # Сигнал: снижение BTC.D > порога за скользящий месяц = ротация в алты.
    # Mozart: «рынок часто идет по цепочке: низкая капа → средняя → высокая»
    #   (пост 10.05.2026).
    # FORMALIZED: порог 2.0 п.п. = значимое смещение баланса BTC/альты.
    #   Дневной шум BTC.D: 0.1–0.5 п.п. Движение > 2 п.п. за месяц = направленная ротация.
    #   При уточнении Mozart — менять только здесь; функция и тесты не трогаются.
    "btc_dominance_rotation_threshold_pct": 2.0,

    # М-15 | Funding Rate 30d MA (пост 11.03.2026) ────────────────────────
    # Источник: пост 11.03.2026 (М-15, PLAN_MOZART_PATTERNS.md ЧАСТЬ 3)
    # Mozart: «30-дневная MA funding rate на многолетнем минимуме = предшествует дну цикла»
    # API: /v1/funding-rate → поле fundingRate (строка, доли; 0.0001 = базовая ставка Binance).
    # Данные: 8-часовой интервал (3 записи/день); диагностика 2026-06-02: 3178 записей с 2023-07-09.
    #
    # funding_rate_ma_window_days: 30 дней × 3 записи/день = 90 записей для MA.
    #   FORMALIZED: Mozart явно называет 30 дней (пост 11.03.2026). При уточнении — менять здесь.
    # funding_rate_ma_floor: порог «многолетнего минимума» в долях (= -0.5% = -0.005).
    #   FORMALIZED: базовая ставка Binance = +0.0001; MA < -0.005 = весь рынок
    #   стабильно платит лонгам, что исторически предшествует дну цикла.
    #   При уточнении данных — менять только здесь.
    "funding_rate_ma_window_days": 30,
    "funding_rate_ma_floor":      -0.005,   # FORMALIZED
}
