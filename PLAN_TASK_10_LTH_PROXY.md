# Задача 10: Интеграция LTH Pain Proxy

**Дата создания:** 2026-04-07
**Статус:** ПЛАН (не начато)
**Файлы затронуты:** `volume_density.py`, `tests/test_volume_density.py`

---

## Контекст и мотивация

Текущий pipeline уже детектирует капитуляцию через `check_capitulation_signal()`
(бинарный True/False) и реальные убытки LTH в USD через BGeometrics API.

**Чего не хватает:** непрерывной шкалы — *насколько глубоко* LTH в убытке
и *как долго* эта фаза длится. Это нужно не для скоринга, а для контекстного
блока в отчёте оркестратора.

**Почему не добавляем в веса `calculate_poc_quality_score()`:**
прокси частично дублирует уже существующий `onchain_score`. Добавление
нового веса не улучшит точность — только создаст корреляцию между компонентами.

---

## Что реализуем

### Шаг 10-1: Чистая функция `calculate_lth_pain_proxy()`

**Файл:** `volume_density.py` (уровень модуля, рядом с другими чистыми функциями)

**Сигнатура:**
```python
def calculate_lth_pain_proxy(
    df_ohlcv: pd.DataFrame,
    window: int = 155,
) -> dict:
```

**Входные данные:**
- `df_ohlcv` — DataFrame с колонками `close`, `vol`, `date`
  (тот же df что уже есть в оркестраторе, после `apply_time_decay`)
- `window` — период в днях (155 = стандартное определение LTH: монеты
  не двигавшиеся > 155 дней)

**Возвращаемый dict:**

| Ключ | Тип | Описание |
|---|---|---|
| `proxy_sopr` | float | `current_price / vwma_155` — аналог SOPR LTH |
| `vwma_155` | float | Volume-Weighted MA за 155 дней (не простая SMA) |
| `phase` | str | Текущая фаза (см. таблицу фаз ниже) |
| `phase_comment` | str | Человекочитаемое описание фазы |
| `roc_14` | float | Изменение proxy_sopr за 14 дней в % (+ = прокси растёт) |
| `days_below_1` | int | Дней подряд с proxy_sopr < 1.0 |

**Таблица фаз:**

| Условие | phase | phase_comment |
|---|---|---|
| proxy > 1.10 | `'BULL'` | LTH в прибыли >10%. Бычий рынок или ранняя коррекция. |
| 1.0 < proxy ≤ 1.10 | `'EARLY_BEAR'` | LTH у безубытка. Рубикон ещё не пройден. |
| 0.80 < proxy ≤ 1.0 | `'RUBICON'` | LTH перешли в убыток. Начало фазы давления продаж. |
| 0.65 < proxy ≤ 0.80 | `'BEAR_PRESSURE'` | Убыток LTH 20–35%. Середина медвежьего цикла. |
| 0.50 ≤ proxy ≤ 0.65 | `'CAPITULATION'` | Убыток LTH 35–50%. Исторически — зона дна (2015, 2018, 2022). |
| proxy < 0.50 | `'EXTREME'` | Убыток LTH >50%. Экстремальная капитуляция, встречалась только в 2011, 2015. |

**Почему VWMA, а не SMA:**
SMA даёт одинаковый вес тихому боковику и панической распродаже с объёмом
×3. VWMA взвешивает по объёму — цена с высокой проторговкой влияет сильнее.
Это ближе к реальному среднему входу LTH-когорты.

**Почему roc_14 важен:**
На V-образных разворотах (март 2020, ноябрь 2022) SMA/VWMA остаётся
инертной ещё 2–3 месяца после фактического дна. Рост прокси при proxy < 1.0
— ранний сигнал разворота *до* того как цена пробьёт vwma_155.

---

### Шаг 10-2: Флаг `LTH_CAPITULATION_ZONE` в `calculate_poc_quality_score()`

**Условие добавления флага:**
```python
# В блоке формирования flags:
if lth_proxy_sopr is not None and lth_proxy_sopr < 0.60:
    flags.append('LTH_CAPITULATION_ZONE')
    # Что означает: цена находится в исторической зоне дна по метрике LTH.
    # Это не торговый сигнал — это контекст для интерпретации POC.
```

**Изменения сигнатуры `calculate_poc_quality_score()`:**
```python
def calculate_poc_quality_score(
    ...,
    lth_proxy_sopr: float = None,   # новый опциональный параметр
) -> dict:
```

**Важно:** флаг НЕ влияет на числовой `score` и НЕ меняет `label`.
Только добавляется в `flags` для информации.

---

### Шаг 10-3: Блок вывода в оркестраторе `liquidity_density_audit()`

После блока `[POC QUALITY SCORE]` добавить новый блок `[LTH PAIN PROXY]`:

```
[LTH PAIN PROXY]
──────────────────────────────────────────────────────────────────
VWMA(155): $71,240  |  Proxy SOPR: 0.847  |  Фаза: RUBICON
Интерпретация: LTH-когорта (монеты >155 дней) в среднем в убытке ~15%.
               Рубикон пройден 23 февраля (47 дней назад).
               Прокси за 14 дней: -2.3% (давление продолжается).
Историческая шкала: [BULL > 1.1] [EARLY_BEAR 1.0–1.1] [►RUBICON◄] [BEAR 0.65–0.8] [CAPITULATION 0.5–0.65] [EXTREME < 0.5]
Примечание: прокси-метрика на основе VWMA(155). Реальный SOPR LTH
            требует UTXO-данных. Используй как фазовый ориентир, не как точный индикатор.
```

---

## TDD план

### RED фаза (писать первым)

Файл: `tests/test_volume_density.py`

**Тест 1:** базовое значение proxy_sopr
```
# df с 200 строками, последняя close = 80_000, VWMA(155) ≈ 70_000
# ожидаем proxy_sopr ≈ 1.14, phase = 'BULL'
```

**Тест 2:** фаза RUBICON
```
# close = 65_000, VWMA(155) ≈ 72_000
# ожидаем proxy_sopr ≈ 0.903, phase = 'RUBICON'
```

**Тест 3:** фаза CAPITULATION
```
# close = 38_000, VWMA(155) ≈ 65_000
# ожидаем proxy_sopr ≈ 0.585, phase = 'CAPITULATION'
```

**Тест 4:** roc_14 корректен
```
# proxy 14 дней назад = 0.90, сейчас = 0.85
# ожидаем roc_14 < 0, то есть прокси снижается
```

**Тест 5:** days_below_1 считается верно
```
# последние 5 строк df: proxy < 1.0, до них proxy > 1.0
# ожидаем days_below_1 = 5
```

**Тест 6:** недостаточно данных (df < window строк)
```
# df с 50 строками, window=155
# ожидаем proxy_sopr = None, phase = 'INSUFFICIENT_DATA'
```

**Тест 7:** флаг LTH_CAPITULATION_ZONE при proxy < 0.60
```
# calculate_poc_quality_score(..., lth_proxy_sopr=0.55)
# ожидаем 'LTH_CAPITULATION_ZONE' в flags
```

**Тест 8:** флаг НЕ добавляется при proxy >= 0.60
```
# calculate_poc_quality_score(..., lth_proxy_sopr=0.75)
# ожидаем 'LTH_CAPITULATION_ZONE' НЕ в flags
```

**Тест 9:** обратная совместимость — lth_proxy_sopr=None не ломает функцию
```
# calculate_poc_quality_score(...) без lth_proxy_sopr
# функция работает как раньше, флаг не добавляется
```

---

## Порядок выполнения

```
1. Написать тесты 1–6 (RED) → pytest показывает FAILED
2. Реализовать calculate_lth_pain_proxy() → GREEN
3. Написать тесты 7–9 (RED) → pytest показывает FAILED
4. Обновить calculate_poc_quality_score() → GREEN
5. Добавить блок вывода в liquidity_density_audit()
6. Прогнать все 217+9 тестов → все GREEN
7. Обновить NEXT_SESSION.md
```

---

## Критерии готовности

- [ ] 9 новых тестов GREEN
- [ ] 217 старых тестов остались GREEN (итого 226)
- [ ] `calculate_lth_pain_proxy()` содержит docstring с объяснением каждого поля
- [ ] Блок `[LTH PAIN PROXY]` выводится в оркестраторе
- [ ] Флаг `LTH_CAPITULATION_ZONE` появляется при proxy < 0.60
- [ ] NEXT_SESSION.md обновлён
